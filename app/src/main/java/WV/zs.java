package WV;

import android.credentials.CreateCredentialRequest;
import android.credentials.CredentialOption;
import android.credentials.GetCredentialRequest;
import android.os.Bundle;
/* compiled from: chromium-SystemWebView.apk-stable-763212001 */
/* loaded from: classes.dex */
public interface zs {
    void a(Bundle bundle, ns nsVar);

    void b(Bundle bundle, os osVar);

    void c(Bundle bundle, os osVar);

    void d(GetCredentialRequest.Builder builder, os osVar);

    void e(Bundle bundle, os osVar);

    void f(CredentialOption.Builder builder);

    void g(CreateCredentialRequest.Builder builder, ns nsVar);
}
